# Database (Monorepo)

Questa cartella centralizza la mappa dati del monorepo.

Source of truth attuale:

- Schema/migrazioni: `config/migrations/`
- Script DB operativi: `config/scripts/db/`

Sottocartelle:

- `migrations/` riferimenti e runbook migrazioni
- `seeds/` riferimenti seed ambiente
- `demo/` note operative reset/seed demo
