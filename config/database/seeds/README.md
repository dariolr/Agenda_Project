# Seeds

Seed applicativo gestito da `agenda_core`.

Comando wrapper monorepo:

- `config/scripts/db/core_seed_demo.sh`

Nota:

- se `config/migrations/seed_data.sql` non esiste, il seed demo fallisce esplicitamente.
