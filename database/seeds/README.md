# Seeds

Seed applicativo gestito da `agenda_core`.

Comando wrapper monorepo:

- `scripts/db/core_seed_demo.sh`

Nota:

- se `agenda_core/migrations/seed_data.sql` non esiste, il seed demo fallisce esplicitamente.
